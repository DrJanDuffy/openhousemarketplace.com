# Public images

Images in this folder are served at `/images/...`.

- **logo/** – Site logo (e.g. `logo.png`, `logo.svg`). Footer uses `logo.png`.
- **og/** – Open Graph / social share images (optional).
- **hero/** – Hero / banner images for pages (optional).

Add your assets and commit; they will be tracked by git.
