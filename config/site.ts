/**
 * Site identity and purpose. Single source of truth for branding and goal alignment.
 * NAP, hours, and business description follow the Google Business Profile (see config/gbp.ts).
 * The site supports the GBP; keep these in sync with the profile.
 */
export const SITE_NAME = 'Open House Market Place'

/** Primary site goal: drives CTA copy and conversion focus. */
export const SITE_PURPOSE = 'Increase appointments for private showings.'
